<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.BB.Material3" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Material baseline -->
        <item name="android:statusBarColor" tools:targetApi="l">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>

        <!-- TextInput style -->
        <item name="textInputStyle">@style/Widget.BB.TextInputLayout</item>
        <item name="materialButtonStyle">@style/Widget.BB.Button</item>
    </style>

    <style name="Widget.BB.TextInputLayout" parent="Widget.Material3.TextInputLayout.FilledBox">
        <item name="boxBackgroundMode">filled</item>
        <item name="boxCornerRadiusTopStart">16dp</item>
        <item name="boxCornerRadiusTopEnd">16dp</item>
        <item name="boxCornerRadiusBottomStart">16dp</item>
        <item name="boxCornerRadiusBottomEnd">16dp</item>
    </style>

    <style name="Widget.BB.Button" parent="Widget.Material3.Button">
        <item name="android:minHeight">48dp</item>
        <item name="cornerRadius">14dp</item>
    </style>
        <!-- Base application theme. -->
        <style name="Theme.BhaloBariwala" parent="Theme.MaterialComponents.DayNight.NoActionBar">
            <!-- Primary branding color. -->
            <item name="colorPrimary">@color/teal_700</item>
            <item name="colorPrimaryVariant">@color/teal_200</item>
            <item name="colorOnPrimary">@android:color/white</item>
            <item name="android:statusBarColor" tools:targetApi="l">@color/teal_700</item>
        </style>

</resources>


